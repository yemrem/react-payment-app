import classes from './Header.module.css';

const Header = () => {
return(
 <div className={classes.body}>
 <h1 className={classes.header}> HEADER COMPONENT</h1>
 </div>   
  );
};

export default Header;